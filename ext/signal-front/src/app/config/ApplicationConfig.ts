import { environment } from 'environment/environment';

export const ApplicationConfig = {
  applicationOwner: 'SignalOne',
  applicationName: 'SignalOne',
  defaultLanguage: environment.defaultLanguage,
};
